
  # Design TransitOps UI

  This is a code bundle for Design TransitOps UI. The original project is available at https://www.figma.com/design/GueLZQzzk98htKfM6dKoHU/Design-TransitOps-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  